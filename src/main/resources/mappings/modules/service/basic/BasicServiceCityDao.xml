<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.service.dao.basic.BasicServiceCityDao">
    
	<sql id="basicServiceCityColumns">
		a.id AS "id",
		a.org_id AS "orgId",
		a.city_code AS "cityCode",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="basicServiceCityJoins">
	</sql>
    
	<select id="get" resultType="BasicServiceCity">
		SELECT 
			<include refid="basicServiceCityColumns"/>
		FROM basic_service_city a
		<include refid="basicServiceCityJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="BasicServiceCity">
		SELECT 
			<include refid="basicServiceCityColumns"/>
		FROM basic_service_city a
		<include refid="basicServiceCityJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="BasicServiceCity">
		SELECT 
			<include refid="basicServiceCityColumns"/>
		FROM basic_service_city a
		<include refid="basicServiceCityJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO basic_service_city(
			id,
			org_id,
			city_code,
			create_by,
			create_date,
			update_by,
			update_date,
			del_flag
		) VALUES (
			#{id},
			#{orgId},
			#{cityCode},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE basic_service_city SET 	
			org_id = #{orgId},
			city_code = #{cityCode},
			update_by = #{updateBy.id},
			update_date = #{updateDate}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE basic_service_city SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>

	<select id="getCityCodesByOrgId" resultType="BasicServiceCity">
		SELECT
		city_code AS cityCode
		FROM
		basic_service_city
		WHERE
		org_id = #{orgId}
	</select>
</mapper>