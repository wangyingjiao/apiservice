<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.service.dao.skill.SerSkillItemCommodityDao">
    
	<sql id="serSkillItemCommodityColumns">
		a.id AS "id",
		a.item_id AS "itemId",
		a.item_name AS "itemName",
		a.commodity_id AS "commodityId",
		a.commodity_name AS "commodityName",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="serSkillItemCommodityJoins">
	</sql>
    
	<select id="get" resultType="SerSkillItemCommodity">
		SELECT 
			<include refid="serSkillItemCommodityColumns"/>
		FROM ser_skill_item_commodity a
		<include refid="serSkillItemCommodityJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="SerSkillItemCommodity">
		SELECT 
			<include refid="serSkillItemCommodityColumns"/>
		FROM ser_skill_item_commodity a
		<include refid="serSkillItemCommodityJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="SerSkillItemCommodity">
		SELECT 
			<include refid="serSkillItemCommodityColumns"/>
		FROM ser_skill_item_commodity a
		<include refid="serSkillItemCommodityJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO ser_skill_item_commodity(
			id,
			item_id,
			item_name,
			commodity_id,
			commodity_name,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{itemId},
			#{itemName},
			#{commodityId},
			#{commodityName},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE ser_skill_item_commodity SET 	
			item_id = #{itemId},
			item_name = #{itemName},
			commodity_id = #{commodityId},
			commodity_name = #{commodityName},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE ser_skill_item_commodity SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	<update id="delSerSkillItemCommodity">
		UPDATE ser_skill_item_commodity SET
		del_flag = #{DEL_FLAG_DELETE}
		WHERE item_id = #{itemId}
		AND del_flag = #{DEL_FLAG_NORMAL}
	</update>

</mapper>