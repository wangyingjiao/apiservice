<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.service.dao.item.SerItemCommodityPersonsDao">
    
	<sql id="serItemCommodityPersonsColumns">
		a.id AS "id",
		a.goods_id AS "goodsId",
		a.critical AS "critical",
		a.quantity AS "quantity",
		a.sort AS "sort",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="serItemCommodityPersonsJoins">
	</sql>
    
	<select id="get" resultType="SerItemCommodityPersons">
		SELECT 
			<include refid="serItemCommodityPersonsColumns"/>
		FROM ser_item_info_goods_persons a
		<include refid="serItemCommodityPersonsJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="SerItemCommodityPersons">
		SELECT 
			<include refid="serItemCommodityPersonsColumns"/>
		FROM ser_item_info_goods_persons a
		<include refid="serItemCommodityPersonsJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.create_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="SerItemCommodityPersons">
		SELECT 
			<include refid="serItemCommodityPersonsColumns"/>
		FROM ser_item_info_goods_persons a
		<include refid="serItemCommodityPersonsJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.create_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO ser_item_info_goods_persons(
			id,
			goods_id,
			critical,
			quantity,
			sort,
			create_by,
			create_date,
			update_by,
			update_date,
			del_flag
		) VALUES (
			#{id},
			#{goodsId},
			#{critical},
			#{quantity},
			#{sort},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE ser_item_info_goods_persons SET
			<if test="critical != null and critical != ''">
				critical = #{critical},
			</if>
			<if test="quantity != null and quantity != ''">
				quantity = #{quantity},
			</if>
			<if test="sort != null and sort != ''">
				sort = #{sort},
			</if>
			update_by = #{updateBy.id},
			update_date = #{updateDate}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE ser_item_info_goods_persons SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>