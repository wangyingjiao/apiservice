<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.service.dao.order.OrderServiceRelationDao">
    
	<sql id="orderServiceRelationColumns">
		a.id AS "id",
		a.order_id AS "orderId",
		a.item_id AS "itemId",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="orderServiceRelationJoins">
	</sql>
    
	<select id="get" resultType="OrderServiceRelation">
		SELECT 
			<include refid="orderServiceRelationColumns"/>
		FROM order_service_relation a
		<include refid="orderServiceRelationJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="OrderServiceRelation">
		SELECT 
			<include refid="orderServiceRelationColumns"/>
		FROM order_service_relation a
		<include refid="orderServiceRelationJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="OrderServiceRelation">
		SELECT 
			<include refid="orderServiceRelationColumns"/>
		FROM order_service_relation a
		<include refid="orderServiceRelationJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO order_service_relation(
			id,
			order_id,
			item_id,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{orderId},
			#{itemId},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE order_service_relation SET 	
			order_id = #{orderId},
			item_id = #{itemId},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE order_service_relation SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>